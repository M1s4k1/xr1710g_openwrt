# upstream-decisions.md — 上游更新裁决账本

记录 **genshanxinli/xr1710g-openwrt** 每次更新时，逐条「接受 / 拒绝 / 待评估」的裁决与理由。

## 怎么用

```bash
scripts/check-upstream.sh              # ① 看上游更新了什么、有无冲突
                                        # ② 按下面模板逐条登记裁决
scripts/check-upstream.sh --baseline   # ③ 裁决完成后，把当前上游 tip 记为已裁决基线
```

`--baseline` 会把上游 tip 存进 `.stage/upstream-baseline`（不进 git）。
下次 `check-upstream.sh` 只报告**基线之后**的新提交，避免重复裁决。

## 状态词汇

| 状态 | 含义 | 后续动作 |
|------|------|---------|
| `accept` | 接受上游改动，本 fork 跟随 | 可能需调 `local/manifest.rules` |
| `reject` | 拒绝，本 fork 维持现状 | 在上游更新时需重新施加差异（rules 覆盖） |
| `pending` | 待评估，需要实机验证或更多信息 | 登记后挂起，不要忘 |
| `n/a` | 与本 fork 无关（如纯 CI / 文档 / 外部 fork 对账） | 无需动作 |

## 决策基准：以快照时间点为准（重要原则）

本 fork 的规则基线是**固定的上游快照**（当前 `1fea1e5`，09-15 16:41；记于 `local/manifest.rules` 头部）。
裁决上游变动时，必须区分该变动**是否在快照时间点之前**：

| 情形 | 处理 |
|------|------|
| 变动**早于/等于**快照时间 ⇒ 属快照内既成事实 | **纳本轮裁决** |
| 变动**晚于**快照时间 ⇒ 快照不含它 | **不纳入**，保持不动；待下次快照推进时再评 |

**为什么**：快照是构建可复现性的锚点。若把快照之后的变动混进来，rules 就变成了"滚动跟上游"，
失去版本确定性 —— 且会让 `--baseline` 的增量语义失真。

**判定方法**：比对变动提交的 author-date 与 `local/manifest.rules` 头部的快照时间。

> 实例（2026-09-17）：`mt76-0014` 的削弱（`774cf55`，09-15 22:05）虽晚于快照，但其**否证证据**
> （F170 判定，09-15 16:41 前）已在快照内 ⇒ 可裁决，已 `reject`。
> 而 `mt76-0023`（09-15 20:14 首次提交）整体晚于快照 ⇒ `n/a`，不动。

## 裁决记录表

新记录**加在表格顶部**（最新在上）。提交号取 7 位短 SHA。

| 提交 | 日期 | 内容摘要 | 状态 | 理由 / 动作 |
|------|------|---------|------|------------|
| `774cf55` | 2026-09-17 | 删 047 full-reset 停 NPU hunk（mt76-0014 由 24 行削至 7 行） | `reject` | **已撤销 0014 提档**（`local/manifest.rules` 由 `^` 改为 `-`）。依据：上游 F170 实机否证其内层 047 修法 —— ci-171（首测超时）、ci-172（A 修法）、ci-174（B 隔离）三版全灭，真根因是 PCIe AER 而非 NPU stop；上游始终留 `#EXP`。快照 `1fea1e5` 内即已含否证证据 ⇒ 属快照内可裁决项。func 档活动补丁 104 → 103（移出 mt76-0014 提档） |
| `18733e5` | 2026-09-17 | 新增 mt76-0023 PCIe AER error handling（#EXP） | `n/a` | 提交时间 09-15 20:14，**晚于本档快照 `1fea1e5`（09-15 16:41）** ⇒ 按「以快照时间点为决策基准」的原则，不纳入本轮考量，保持不动。上游后续 `df8c7ad` 已把它改为 fail-fast + 10s 受控重启的成熟形态，待下次快照推进时评估 |
| `675a185` | 2026-09-17 | 新增 9059a/9059b（SOE/TRNG DTS 载体，均 #EXP） | `n/a` | 我**停用了整个 SOE 组**（32 条停用中含 9999-net-airoha-soe-*）⇒ 与我的选择一致，无需动作 |

## 2026-09-17 首轮裁决（34 个提交）

**上游基线**：`1fea1e5` → **远端**：`244d433`（34 提交）

**脚本结论**：检出 1 处 HIT（`mt76-0014`），无撞号，无新增默认档补丁。

**分类归纳**（34 个提交的实际构成）：

| 类别 | 数量 | 对本 fork 的影响 |
|------|------|-----------------|
| `docs(fixes)` / `docs(watch)` / `docs(validation)` 文档与账本 | 多数 | 无。纯记录，不改补丁行为 |
| `fix(patches)` / `fix(dts)` / `fix(ppe)` 补丁修复 | 5 | 均为**已有补丁**的锚点/上下文修复 ⇒ 我跟随上游即可，无需改 rules |
| `feat(dts)` 新增 9059a/9059b | 1 | #EXP 档，我停用 SOE 组 ⇒ 不进我的档 |
| `chore(audit)` 外部 fork 对账 | 2 | 无 |
| 唯一需关注 | 1 | `mt76-0014`（见上表 `reject`，已撤销提档） |

**净影响**：func 档活动补丁 **104 → 103**（移出 mt76-0014 提档，其上游基线为 #EXP，提档才进 active）；其余补丁的上游改动都是"让它们更准"而非"改变行为"。

**本轮已闭环**：0014 撤销提档已落地并验证（audit-local PASSED、audit-order 一致：合成 MANIFEST 共 140 条 / default 103）。

**未纳入本轮**：`mt76-0023`（PCIe AER 真修复）提交于 09-15 20:14，晚于快照 `1fea1e5`（09-15 16:41）。按「以快照时间点为决策基准」原则保持不动；下次快照推进时应优先评估它。

---

## 裁决要留意的三类信号

`check-upstream.sh` 会自动标出这三类，见到就要停下来想：

1. **`⚠ HIT`** — 上游动了我停用/提档的补丁
   - 我停用（`-`）的：上游改了它 ⇒ 我的"不用它"的理由可能变了（比如上游修好了）
   - 我提档（`^`）的：上游改了它 ⇒ 我提档的依据可能被动摇（**本轮的 mt76-0014 就是这类**）

2. **`⚠ COLLIDE`** — 上游新增补丁编号与我的本地补丁（`9501+`）撞号
   - 处理：改我的号（我的命名空间是 9501+，与上游 9000-9099 隔离但需检查）

3. **新增·默认档** — 上游往默认档加了补丁
   - 这个**默认就被我继承**（我的 rules 是在上游 default 基础上做增删）
   - 需判断：这个新补丁我是否要**停用**（若它带来我不想要的行为）

## 与上游自有机制的关系

上游 genshanxinli 已自建三层守护，本账本是**第四层**，各管一段：

| 层 | 归属 | 追踪对象 |
|----|------|---------|
| L1 `audit-upstream-watch.sh` | 上游 | 本体补丁所基于的 **openwrt 上游文件** blob 漂移 |
| L2 `REMOTE_TIPS`（同文件） | 上游 | **6 个外部 fork**（YYH / fanboy / Gilly / naoki66 等）tip 变化 |
| L3 `sync-upstream.yml` (2h cron) | 上游 | openwrt master 同步 + 补丁层 dry-run |
| **L4 本账本** | **本 fork** | **genshanxinli 自身**的更新对我的选择集意味着什么 |

L1-L3 关心"上游对不对"，L4 关心"上游的变动**要不要跟进到我的 fork**"。层与层不重叠。
